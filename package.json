{
  "name": "veterannews",
  "version": "1.0.0",
  "description": "Veteran News — Ground truth for those who served.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "deploy:scraper": "wrangler deploy --config wrangler-scraper.toml"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
